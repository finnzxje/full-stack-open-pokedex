name: Hello world!
on:
  push:
    branches:
      - main
jobs:
  hello_world_jobs:
    runs-on: ubuntu-22.04
    steps:
      - name: Say Hello
        run: |
          echo "Hello World!"
      - name: Now it is
        run: |
          date
      - name: Directory content
        run: ls -l
